# TCP port of the REST API server
rest_api_port = 9999
# LAN interface towards the internal (user) network
lan_interface = "eth1"
# WAN interface towards the external (gateway) network
wan_interface = "eth2"
# Boolean to say if a new rule must be created with quick keyword
quick_rules = "True"
